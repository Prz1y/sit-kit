diff --git a/network-test-script/Performance Tuning for ConnectX-6 Dx on Hygon 74xx cpus/Deprecated scripts/network_tune_test_v2.sh b/network-test-script/Performance Tuning for ConnectX-6 Dx on Hygon 74xx cpus/Deprecated scripts/network_tune_test_v2.sh
index c031e92..ac9dafc 100644
--- a/network-test-script/Performance Tuning for ConnectX-6 Dx on Hygon 74xx cpus/Deprecated scripts/network_tune_test_v2.sh	
+++ b/network-test-script/Performance Tuning for ConnectX-6 Dx on Hygon 74xx cpus/Deprecated scripts/network_tune_test_v2.sh	
@@ -1,5 +1,8 @@
 #!/bin/bash
 
+set -euo pipefail
+
+
 # 颜色输出
 RED='\033[0;31m'
 GREEN='\033[0;32m'
@@ -14,6 +17,11 @@ QUEUES_PER_NIC=0
 LOG_DIR="/tmp/iperf_logs"
 TIMESTAMP=""
 SERVER_PIDS=()
+AUTO_YES=${AUTO_YES:-0}
+MODE_ARG=${1:-}
+SERVER_IP_ARG=${2:-}
+DURATION_ARG=${3:-}
+ADDED_RAW_RULES=()
 
 # 日志函数
 log_info() { echo -e "${GREEN}[INFO]${NC} $1"; }
@@ -31,15 +39,13 @@ cleanup() {
     for pid in "${MONITOR_PIDS[@]}"; do
         kill "$pid" 2>/dev/null || true
     done
-    sudo pkill -x iperf 2>/dev/null || true
     # 恢复 irqbalance
-    sudo systemctl enable irqbalance 2>/dev/null || true
-    sudo systemctl start irqbalance 2>/dev/null || true
-    log_info "已恢复 irqbalance 服务"
-    # 恢复 NUMA balancing
+    if systemctl list-unit-files irqbalance.service >/dev/null 2>&1; then
+        sudo systemctl start irqbalance 2>/dev/null || true
+        log_info "已恢复 irqbalance 服务"
+    fi
     sudo sysctl -w kernel.numa_balancing=1 &>/dev/null || true
-    # 清理 conntrack NOTRACK 规则
-    sudo iptables -t raw -F 2>/dev/null || true
+    remove_added_raw_rules
     log_info "清理完成"
 }
 trap cleanup EXIT INT TERM
@@ -149,17 +155,59 @@ select_nearest_numa() {
 #############################################################################
 run_cmd() {
     local cmd="$1"
+    local tmp_script
+    tmp_script=$(mktemp)
+    printf "#!/bin/bash
+set -e
+%s
+" "$cmd" > "$tmp_script"
+    chmod +x "$tmp_script"
     echo -n -e "  执行: ${cmd} ... "
-    if eval "sudo $cmd" >/dev/null 2>&1; then
+    if sudo bash "$tmp_script" >/dev/null 2>&1; then
         echo -e "${GREEN}[成功]${NC}"
     else
         echo -e "${RED}[失败或不支持]${NC}"
     fi
+    rm -f "$tmp_script"
 }
 
 #############################################################################
 # 5.1 生成 CPU hex 位掩码
 #############################################################################
+write_root_file() {
+    local value="$1"
+    local target_file="$2"
+    printf "%s\n" "$value" | sudo tee "$target_file" >/dev/null
+}
+
+set_all_governors_performance() {
+    local gov
+    for gov in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do
+        [ -f "$gov" ] || continue
+        write_root_file "performance" "$gov" 2>/dev/null || true
+    done
+}
+
+add_raw_notrack_rule() {
+    local chain="$1"
+    local proto_field="$2"
+    local port_range="$3"
+    if command -v iptables >/dev/null 2>&1; then
+        sudo iptables -t raw -C "$chain" -p tcp "$proto_field" "$port_range" -j NOTRACK 2>/dev/null || {
+            sudo iptables -t raw -A "$chain" -p tcp "$proto_field" "$port_range" -j NOTRACK 2>/dev/null && ADDED_RAW_RULES+=("$chain|$proto_field|$port_range")
+        }
+    fi
+}
+
+remove_added_raw_rules() {
+    local rule
+    for rule in "${ADDED_RAW_RULES[@]:-}"; do
+        [ -n "$rule" ] || continue
+        IFS="|" read -r chain proto_field port_range <<< "$rule"
+        sudo iptables -t raw -D "$chain" -p tcp "$proto_field" "$port_range" -j NOTRACK 2>/dev/null || true
+    done
+}
+
 cpu_to_mask() {
     local cpu=$1
     local total_cpus
@@ -190,7 +238,7 @@ setup_route_tuning() {
         local existing
         existing=$(ip route show "$subnet" 2>/dev/null | head -1)
         if [ -n "$existing" ]; then
-            sudo ip route change $existing initcwnd 128 initrwnd 128 2>/dev/null || true
+            sudo bash -c "ip route change $existing initcwnd 128 initrwnd 128" 2>/dev/null || true
             log_info "  $subnet: initcwnd=128 initrwnd=128"
         fi
     done
@@ -210,7 +258,7 @@ start_monitoring() {
     ethtool -S "$nic2" > "${LOG_DIR}/${nic2}_ethtool_S_before.log" 2>/dev/null || true
     if command -v sar &>/dev/null; then
         sar -n DEV 5 $((duration / 5 + 1)) > "${LOG_DIR}/sar_net.log" 2>&1 &
-        MONITOR_PIDS+=($!)
+        MONITOR_PIDS+=("$!")
     fi
 }
 
@@ -263,7 +311,6 @@ stop_monitoring() {
     log_info "完整诊断日志: ${LOG_DIR}/"
 }
 
-MONITOR_PIDS=()
 
 #############################################################################
 # 6.3 防火墙放通 iperf 端口
@@ -321,7 +368,7 @@ optimize_all() {
     # 确保 irqbalance 进程彻底终止 (防止残留进程覆盖 IRQ 亲和性)
     sudo pkill -9 irqbalance 2>/dev/null || true
     sleep 0.5
-    run_cmd "bash -c 'for gov in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do echo performance > \$gov 2>/dev/null || true; done'"
+    set_all_governors_performance
 
     log_info "基础网络与 TCP 参数优化..."
     run_cmd "sysctl -w net.core.rmem_max=2147483647"
@@ -361,7 +408,8 @@ optimize_all() {
 
         local cpus_str
         cpus_str=$(get_numa_cpus "$this_numa")
-        local cpus_all=($cpus_str)
+        local -a cpus_all=()
+        read -r -a cpus_all <<< "$cpus_str"
         local total=${#cpus_all[@]}
 
         local bind_cpus
@@ -384,7 +432,8 @@ optimize_all() {
         if [ "$queues" -gt 16 ]; then
             queues=16
             # 截取前 16 个 CPU 用于绑核
-            local cpu_tmp=($bind_cpus)
+            local -a cpu_tmp=()
+            read -r -a cpu_tmp <<< "$bind_cpus"
             bind_cpus="${cpu_tmp[*]:0:16}"
         fi
 
@@ -414,18 +463,19 @@ optimize_all() {
         pci_dev=$(basename "$(readlink -f /sys/class/net/${nic}/device)" 2>/dev/null)
         local irqs=()
         if [ -n "$pci_dev" ]; then
-            irqs=($(grep "mlx5_comp.*${pci_dev}" /proc/interrupts | awk -F: '{print $1}' | tr -d ' '))
+            mapfile -t irqs < <(grep "mlx5_comp.*${pci_dev}" /proc/interrupts | awk -F: '{print $1}' | tr -d ' ' || true)
             if [ ${#irqs[@]} -eq 0 ]; then
-                irqs=($(grep "${pci_dev}" /proc/interrupts | awk -F: '{print $1}' | tr -d ' '))
+                mapfile -t irqs < <(grep "${pci_dev}" /proc/interrupts | awk -F: '{print $1}' | tr -d ' ' || true)
             fi
         fi
-        local cpu_arr=($bind_cpus)
+        local -a cpu_arr=()
+        read -r -a cpu_arr <<< "$bind_cpus"
         local count=${#cpu_arr[@]}
         local idx=0
         local success_irq=0
         for irq in "${irqs[@]}"; do
             local target_cpu=${cpu_arr[$((idx % count))]}
-            if sudo bash -c "echo '${target_cpu}' > /proc/irq/${irq}/smp_affinity_list" 2>/dev/null; then
+            if write_root_file "$target_cpu" "/proc/irq/${irq}/smp_affinity_list" 2>/dev/null; then
                 success_irq=$((success_irq + 1))
             fi
             idx=$((idx + 1))
@@ -445,7 +495,7 @@ optimize_all() {
             if [ -f "$xps_file" ]; then
                 local mask
                 mask=$(cpu_to_mask "$cpuid")
-                if sudo bash -c "echo '$mask' > '$xps_file'" 2>/dev/null; then
+                if write_root_file "$mask" "$xps_file" 2>/dev/null; then
                     xps_ok=$((xps_ok + 1))
                 fi
             fi
@@ -469,7 +519,7 @@ optimize_all() {
         local rps_cleared=0
         for f in /sys/class/net/${nic}/queues/rx-*/rps_cpus; do
             if [ -f "$f" ]; then
-                sudo bash -c "echo 0 > '$f'" 2>/dev/null && rps_cleared=$((rps_cleared + 1))
+                write_root_file 0 "$f" 2>/dev/null && rps_cleared=$((rps_cleared + 1))
             fi
         done
         echo -e "  [RPS] $nic 已清除 ${rps_cleared} 个队列的 RPS ... ${GREEN}[完成]${NC}"
@@ -482,19 +532,12 @@ optimize_all() {
     log_info "为 iperf 端口绕过 conntrack 连接跟踪..."
     if command -v iptables &>/dev/null; then
         # 先清理旧规则 (避免重复)
-        sudo iptables -t raw -D PREROUTING -p tcp --dport 5001:5100 -j NOTRACK 2>/dev/null || true
-        sudo iptables -t raw -D PREROUTING -p tcp --dport 6001:6100 -j NOTRACK 2>/dev/null || true
-        sudo iptables -t raw -D OUTPUT -p tcp --sport 5001:5100 -j NOTRACK 2>/dev/null || true
-        sudo iptables -t raw -D OUTPUT -p tcp --sport 6001:6100 -j NOTRACK 2>/dev/null || true
-        sudo iptables -t raw -D PREROUTING -p tcp --sport 5001:5100 -j NOTRACK 2>/dev/null || true
-        sudo iptables -t raw -D PREROUTING -p tcp --sport 6001:6100 -j NOTRACK 2>/dev/null || true
-        # 添加 NOTRACK 规则
-        sudo iptables -t raw -A PREROUTING -p tcp --dport 5001:5100 -j NOTRACK 2>/dev/null || true
-        sudo iptables -t raw -A PREROUTING -p tcp --dport 6001:6100 -j NOTRACK 2>/dev/null || true
-        sudo iptables -t raw -A PREROUTING -p tcp --sport 5001:5100 -j NOTRACK 2>/dev/null || true
-        sudo iptables -t raw -A PREROUTING -p tcp --sport 6001:6100 -j NOTRACK 2>/dev/null || true
-        sudo iptables -t raw -A OUTPUT -p tcp --sport 5001:5100 -j NOTRACK 2>/dev/null || true
-        sudo iptables -t raw -A OUTPUT -p tcp --sport 6001:6100 -j NOTRACK 2>/dev/null || true
+        add_raw_notrack_rule PREROUTING --dport 5001:5100
+        add_raw_notrack_rule PREROUTING --dport 6001:6100
+        add_raw_notrack_rule PREROUTING --sport 5001:5100
+        add_raw_notrack_rule PREROUTING --sport 6001:6100
+        add_raw_notrack_rule OUTPUT --sport 5001:5100
+        add_raw_notrack_rule OUTPUT --sport 6001:6100
         log_info "  已为端口 5001-5100 / 6001-6100 绕过 conntrack"
     fi
 
@@ -549,8 +592,6 @@ setup_client_ips() {
 run_server() {
     local app_numa=$1
     log_info "启动 iperf 服务端 (多实例模式, NUMA $app_numa)..."
-    sudo pkill -x iperf 2>/dev/null || true
-    sleep 1
     mkdir -p "$LOG_DIR"
     local nic1=${NICS[0]}
     local nic2=${NICS[1]}
@@ -558,7 +599,8 @@ run_server() {
     # 获取 APP NUMA 的 CPU 列表
     local app_cpus_str
     app_cpus_str=$(get_numa_cpus "$app_numa")
-    local app_cpus=($app_cpus_str)
+    local -a app_cpus=()
+    read -r -a app_cpus <<< "$app_cpus_str"
     local num_app_cpus=${#app_cpus[@]}
 
     # ★ 实例数固定 3: 实测 7 进程×2 流开销过大 (进程调度 + 系统调用),
@@ -572,7 +614,7 @@ run_server() {
         local cpu=${app_cpus[$((j % num_app_cpus))]}
         taskset -c "$cpu" \
             iperf -s -p "$port" > "${LOG_DIR}/${nic1}_server_p${j}.log" 2>&1 &
-        SERVER_PIDS+=($!)
+        SERVER_PIDS+=("$!")
     done
     sleep 1
 
@@ -583,7 +625,7 @@ run_server() {
         local cpu=${app_cpus[$(( (j + num_inst) % num_app_cpus ))]}
         taskset -c "$cpu" \
             iperf -s -p "$port" > "${LOG_DIR}/${nic2}_server_p${j}.log" 2>&1 &
-        SERVER_PIDS+=($!)
+        SERVER_PIDS+=("$!")
     done
     sleep 1
 
@@ -615,7 +657,11 @@ check_connectivity() {
     fi
     if [ "$ok" = false ]; then
         log_warn "部分链路不通，是否继续测试？(y/N)"
-        read -r ans
+        if [ "$AUTO_YES" -eq 1 ]; then
+            ans="y"
+        else
+            read -r ans
+        fi
         if [[ ! "$ans" =~ ^[yY] ]]; then
             log_error "用户取消测试"
         fi
@@ -638,14 +684,13 @@ run_client() {
 
     check_connectivity "$server_ip" "$server_ip2"
 
-    sudo pkill -x iperf 2>/dev/null || true
-    sleep 1
     mkdir -p "$LOG_DIR"
 
     # ★ 多端口多实例: 客户端和服务端各 N 个进程, 一一对应, 消除双端锁竞争
     local app_cpus_str
     app_cpus_str=$(get_numa_cpus "$app_numa")
-    local app_cpus=($app_cpus_str)
+    local -a app_cpus=()
+    read -r -a app_cpus <<< "$app_cpus_str"
     local num_app_cpus=${#app_cpus[@]}
     # ★ 3 进程 × 5 流 = 15 流/口: 最佳平衡
     #   - 3 进程: 最小化进程调度和系统调用开销
@@ -672,7 +717,7 @@ run_client() {
             iperf -c "${server_ip}" -p "$port" -P "$flows_per_inst" \
             -t "$duration" -i 5 -w 4m -N \
             > "${LOG_DIR}/${nic1}_pair1_p${j}.log" 2>&1 &
-        all_pids+=($!)
+        all_pids+=("$!")
     done
     sleep 1
 
@@ -685,7 +730,7 @@ run_client() {
             iperf -c "${server_ip2}" -p "$port" -P "$flows_per_inst" \
             -t "$duration" -i 5 -w 4m -N \
             > "${LOG_DIR}/${nic2}_pair2_p${j}.log" 2>&1 &
-        all_pids+=($!)
+        all_pids+=("$!")
     done
 
     # 等待所有进程完成
@@ -792,7 +837,14 @@ main() {
     echo "选择运行模式:"
     echo "  1) 服务端 (Server)"
     echo "  2) 客户端 (Client)"
-    read -p "请输入选择 [1-2]: " mode
+    local mode="${1:-${MODE_ARG:-}}"
+    if [ -z "$mode" ]; then
+        echo ""
+        echo "选择运行模式:"
+        echo "  1) 服务端 (Server)"
+        echo "  2) 客户端 (Client)"
+        read -r -p "请输入选择 [1-2]: " mode
+    fi
 
     case $mode in
         1)
@@ -805,10 +857,16 @@ main() {
             ;;
         2)
             setup_client_ips
-            read -p "请输入服务器 IP (Pair1) [默认: 192.168.100.2]: " server_ip
-            server_ip=${server_ip:-192.168.100.2}
-            read -p "测试时长(秒) [默认: 60]: " duration
-            duration=${duration:-60}
+            local server_ip="${2:-${SERVER_IP_ARG:-192.168.100.2}}"
+            local duration="${3:-${DURATION_ARG:-60}}"
+            if [ -z "${SERVER_IP_ARG:-}" ] && [ "$AUTO_YES" -ne 1 ]; then
+                read -r -p "请输入服务器 IP (Pair1) [默认: 192.168.100.2]: " server_ip
+                server_ip=${server_ip:-192.168.100.2}
+            fi
+            if [ -z "${DURATION_ARG:-}" ] && [ "$AUTO_YES" -ne 1 ]; then
+                read -r -p "测试时长(秒) [默认: 60]: " duration
+                duration=${duration:-60}
+            fi
             run_client "$server_ip" "$APP_NUMA" "$duration"
             ;;
         *)
